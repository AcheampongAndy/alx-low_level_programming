#include "main.h"
/**
 * mul - multiplies two integers
 * @a: a first number
 * @b: a second number
 *
 * Return: result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
