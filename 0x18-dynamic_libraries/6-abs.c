#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @num: the number to find its absolute
 *
 * Return: num
 */
int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
